
function dependentes(elemento){

    let img = elemento;
    let id = $(img).attr("id");

   $.$.ajax({
    type: "method",
    url: "url",
    data: "data",
    dataType: "dataType",
    success: function (response) {
        
    }
   });
}